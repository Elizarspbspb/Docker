FROM golang:latest
# FROM - на основании какого образа собираем 
COPY ./ ./
# Копируем все локальные файлы из директории в файловую систему образов
RUN go build -v -o main ./main.go
# Выполнение команд
CMD ["./main"]
# CMD - точка входа для контейнера. Запуск после сборки.